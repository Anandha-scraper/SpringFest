"use client";

import Layout from "@/components/layout/Layout.jsx";

/** The marketing chrome — navbar and (on the landing page) footer. */
export default function MarketingLayout({ children }) {
  return <Layout>{children}</Layout>;
}
